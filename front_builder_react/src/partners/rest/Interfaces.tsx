
export interface InitSearch {
    total: number,
    columns: string[],
    products: string[]
}

export interface DataSearch {
    data: any, //must see
    total: number
}

export interface ConsoSearch {
    data: any, //must see
    total: number
}
