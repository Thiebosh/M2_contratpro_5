import './Project.scss';

export default function Project() {
    return (
        <div id="project">
            Project page
            display one project
            - needs (with compile)
            - prototype
            - settings
        </div>
    );
}
